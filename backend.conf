terraform {
  backend "azurerm" {
    resource_group_name  = "NEWRG"
    storage_account_name = "azurekssvcd"
    container_name       = "terraform"
    key                  = "terraform.tfstate"
  }
}
